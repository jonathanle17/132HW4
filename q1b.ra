\rename_{name, loanCount} (
    \aggr_{name: count(lno)} (
        \rename_{name, lno, due} borrower
    )
)
\union
\rename_{name, loanCount} (
    \project_{name, 0} (
        \project_{name} customer \diff \project_{name} (
            \rename_{name, lno, due} borrower
        )
    )
);